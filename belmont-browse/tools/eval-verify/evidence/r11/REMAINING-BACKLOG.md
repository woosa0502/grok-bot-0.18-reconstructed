# 남은 백로그 — 쉬운 정리 (2026-09-14)

> 한 줄 요약: **큰 기능은 다 검증됐고, 남은 건 3종류뿐** — ①재료가 없어서 못 하는 것,
> ②기능 자체가 아예 없는 것, ③테스트를 더 넓게 만들면 되는 것.
>
> **GPT-6 Pro와 이미 상호 합의(합의) 완료** (round-13): "지금 상태 분류가 코드·증거와 정확히 맞다"에 동의.
> 다만 "제품 전부 완료"는 아니며, 아래가 남은 목록이다. 검토 기준 커밋 = `2ae8745`
> (github.com/woosa0502/grok-bot-0.18-reconstructed, 브랜치 feat/skill-scope-fields).

비유: 자동차 검사와 같다. 엔진·브레이크(핵심)는 다 통과. 남은 건 "특수 장비가 있어야 볼 수 있는 부분",
"아직 안 달린 부품", "문 하나만 열어봤고 나머지 문은 더 열어봐야 하는 것".

---

## 1. 현재 합의된 성적표 (세 버킷 — 서로 합산하지 않음)

| 버킷 | 항목 |
|------|------|
| **완전 검증 (VERIFIED)** | L13, L15, L26, L16-멱등, recovery(복구), 봇 위임생성(에이전트·루틴), L47(실제 daemon 수명주기), L48(설치 동일성/키), L12·L27(오프라인 격리) |
| **부분 검증 (PARTIAL)** | L02, L10, L16-durable, L38, L46, L49, L50 |
| **제외/막힘 (EXCLUDED·BLOCKED)** | Telegram, autopost, L37, L41, L42(+민감파일 deny-list 갭) |

> 원칙: 세 버킷을 하나의 "완료 개수"로 합치지 않는다. 부분·제외·막힘은 완료에 더하지 않는다.

---

## 2. 남은 것 한눈에 (부분 검증 + 제외/막힘)

| 항목 | 지금까지 된 것 | 왜 안 끝났나 | 끝내려면 |
|---|---|---|---|
| **L02 브라우저 도구** | 폼 1개: 주소이동·읽기·입력·클릭 됨 | 서버가 값 받은 것만으론 "브라우저가 진짜 눌렀다"를 증명 못 함(브라우저 없이 직접 요청해도 통과 가능). 탭 2개·긴 페이지·드래그·키보드·좌표클릭·잘못된 탭·오래된 참조는 안 봄 | 세션의 도구 호출 기록까지 연결 + 나머지 동작 테스트 |
| **L10 하위일꾼 격리·결과 모으기** | 백그라운드 방식 완전 검증(일꾼 2명 구분·결과 안 섞임·실패를 성공으로 세탁 안 함·등록일꾼 대조) | 인라인 방식은 안전하게 "확인 불가"로 둠(자동통과 금지). 부모 보고 판정이 정규식이라 표현 다양성에 한계 | (선택) 인라인 연결근거(Task 인자) 추출 |
| **L16 durable-accept** | 세션 기록이 재시작을 넘어 보존됨 | 같은 요청 동시전송·응답유실·다른요청 큐적재·중복방지 원장은 안 봄 | 수락경로 전체 테스트 추가 |
| **L38 첨부 해시/중복제거** | 실제 저장블록 2만439개 확인(내용주소화·중복 0). 소스 해석도 정정(저장시 강제 아님, 생산자 계약) | 실제 첨부 **업로드 경로**(같은 이름 다른 내용·재시도·조각 조회·잘못된 경로·원본↔조회 해시 대조)는 안 봄 | 첨부 API 왕복 테스트 |
| **L46 빌드/출처·중복소유** | 계약시험 36/36 통과(빌드 신원, staging 변조 거부, 중복 소유 1회 처리) | 픽스처가 4KB 바이트배열·테스트 문자열이라 진짜 Chrome 빌드 아님. "깨끗한 프로필→진짜 빌드→기동 PID·바이너리·출처 일치" 실제 연결은 안 함 | 실제 개발 빌드·기동의 manifest·로그·PID 연결 |
| **L49 데몬 패치/앵커** | 잘못된 앵커 거부·문법검사·기동+동작(라이브 r10 인용) 됨 | **재적용 바이트불변** 못 증명 — 깨끗한 원본 데몬이 저장소에 없음(공급 대기). + 트리의 patch-daemon.py가 shipped 909 번들에 재적용 불가(메모리훅 버전 불일치 finding, 런타임 결함은 아님) | 깨끗한 원본 909 데몬 확보 |
| **L50 업데이트/재개** | 종료소유·drain·재개 계약 통과(실패 시 마커 유지·1회 재시도·중복작업 없음) | **실제 서명된 업데이트 패키지로 바이너리 교체**는 못 함(패키지 없음) | 테스트용 서명 업데이트 패키지 공급 |
| **L37 금고/비밀** | 코드는 있음(설치키·ECDH·보안저장) | 실제 자격·페어링된 기기 필요 | 실기기 페어링·자격 |
| **L41 PWA 전송원장 / L42 PWA 파일정책** | 격리 테스트 경로는 존재 | 실제 페어링 필요. **L42는 민감파일 deny-list가 없는 갭**(수정 또는 명시적 위험수용 필요) | 실기기 페어링 + L42 deny-list 결정 |
| **Telegram, autopost** | — | **기능이 아예 안 만들어짐**(재구축에 어댑터/발행 파이프라인 없음) | 새로 구현 |

---

## 3. 세 가지 이유로 묶으면

- **A. 재료가 없어서 (공급 대기):** L49(깨끗한 원본 데몬), L50(서명 업데이트 패키지), L37·L41·L42(페어링된 실기기·자격)
- **B. 기능이 아예 없어서 (미구현):** Telegram 어댑터, autopost 파이프라인
- **C. 테스트를 더 넓게 만들면 되는 것 (지금 바로 가능):** L02 전체 동작 행렬, L38 첨부 업로드 경로,
  L16 수락경로 전체, L46 실제 빌드→기동 연결

---

## 4. 다음 행동 (사용자 결정)

1. **바로 진행 가능(C 묶음):** 말만 하면 L02·L38·L16·L46 테스트 확장을 이어서 진행.
2. **재료 주기(A 묶음):** 깨끗한 원본 909 데몬 / 서명 업데이트 패키지 / 페어링된 실기기가 있으면 L49·L50·L37·L41·L42 마무리.
3. **구현까지(B 묶음):** "Telegram·autopost 구현해라" 하면 새로 만듦.
4. **여기서 마무리:** 현재 합의된 성적표를 최종본으로 확정.

---

## 참고 — 증거·드라이버 위치

- 성적표 원본: `belmont-browse/tools/eval-verify/evidence/r10/MAP-DISPOSITION.md`
- r11 상세: `belmont-browse/tools/eval-verify/evidence/r11/README.md`, `.../ops/README.md`
- 개별 증거(JSON): `belmont-browse/tools/eval-verify/evidence/r11/ev-*.json`, `.../ops/*.txt|json`
- 판정 드라이버: `belmont-browse/tools/eval-verify/*.mjs`, `.../l10-verify-transcript.py`(--selftest 13/13), `.../l49-daemon-patch-contract.py`
- GPT 합의 스레드: chatgpt 대화 `6aa64783` (제목 "라이브 검증 판정"), round-13에서 합의 확정
