# 인수인계 — 2026-09-03 (세션 종료 시점)

## 지금 떠 있는 것과 띄우는 법
| 구성요소 | 상태 | 띄우기 |
|---|---|---|
| Belmont 앱 (WSL) | `SAND_ASIDE_BROWSE=1 node scripts/run-wsl.mjs`, 게이트웨이 포트는 `sand-data/gateway.json` | `belmont-browse/.state/scripts/restart-belmont.sh` (PWA도 같이 재시작) |
| 브라우저 서비스 (belmont-browse) | 포트 9340, 엔진 902, 기본 모델 `gpt-5.6-luna/max`, 화면 `:0`, 파이프 CDP | README의 시작 명령. 상태 `.state/serve.json` |
| 폰 PWA (새 것) | `grok-mobile-belmont-pwa/server.mjs` 포트 4173, 페어링 643312, 세션 파일 `.sessions.json` | `belmont-browse/.state/scripts/restart-pwa.sh` (옛 PWA: `restart-pwa-legacy.sh`) |
| 폰 접속 | `https://desktop-ofkcvog.tail23bc25.ts.net:8444` (Windows tailscale serve → 4173) | 변경 없음 |
| DeepSeek 봇 "딥시크" | id `0ba28117-d58c-42d0-bc50-976d5ff9da39`, 모델 `nvidia/deepseek-ai/deepseek-v4-flash-0731`, 키는 앱 비밀 저장소 `NVIDIA_API_KEY` | 동작하나 호출당 2분+ (무료 대기열). 지연 기록 `.state/nv-latency.log` |

## 오늘 Belmont에 커밋한 것 (main)
- `2267dba` 지식 저장소 + 모든 봇 `knowledge_search` 도구
- `d70bba5` `knowledge_search read_path` (Read가 sand-data를 거부하므로 본문 반환)
- `3fd1a21` 브라우저 봇: 새 작업 실패 시 gpt-5.5로 1회 재시도
- `6c3d44b` 브라우저 봇: 옛 세션 이어가기 실패 시 새 세션
- `212c731` 침묵 종료 수정: 실행기 메시지 게터 연결(마무리 재촉이 처음으로 동작), 판정 일반화, 압축 중 인지
- `7e658bb` 로컬 문맥 요약을 `gpt-5.6-luna`로; 봇 모델 id `nvidia/…` → NVIDIA NIM 라우팅
- `ef3f00d` NaN 토큰 정리; 라우팅 봇의 평문 답 자동 전달
테스트 298개 통과. 런타임은 커밋 뒤 `node scripts/setup-wsl.mjs`로 재빌드해야 기동 검사를 통과한다.

## git 밖에 있는 것
- `belmont-browse/` : 약속대로 커밋 안 함 (`.git/info/exclude`)
- `grok-mobile-belmont-pwa/` : 사용자 작업 폴더. 빌드 지문이 미추적 파일까지 재서 `.git/info/exclude`에 넣음. 정식 PWA로 굳힐지(커밋 + `belmont-mobile-pwa/` 제거) 사용자 결정 대기
- 매니저 루틴 "Job sweeper" 프롬프트 축소 (`automations/job-sweeper/automation.json`, 백업 `.bak-20260903`)

## 오늘 PWA(`grok-mobile-belmont-pwa/`)에 고친 것
- 보낸 메시지 두 개 표시 → `src/entries.ts` 조정 + 서버가 `clientNonce` 전달
- 작업 중 캐릭터 줄(`작업 중`) + 2.5초 상태 갱신
- 세션 파일 저장(재시작해도 페어링 유지), 알림 화면 1회만
- 키보드 뒤 화면 밀림: `interactive-widget=resizes-content`, 문서 스크롤 잠금, 화면 전환 시 scrollTo(0,0)
- 아래 여백: 시스템 바 높이를 더함
`npm run check` 21개 통과. 마지막 "밑에 잘림" 수정은 폰 확인 대기.

## 남은 과제 (우선순위 순)
1. PWA 컴퓨터 화면(noVNC) 실제 프레임·커서·터치 검증 — 원래 불만의 핵심
2. Belmont 유효 문맥 창 5만 토큰 핀 재측정 (한글 토큰 추정 2.5자/토큰 vs 백엔드 실제 사용량)
3. 느린 경로 봇(NVIDIA)에서 "먼저 인사" 숨은 재촉 끄기; NVIDIA 대기열이 풀리는 시간대 확인
4. 요약 프롬프트에 "사용자가 아직 결과를 못 받음" 명시
5. 학습 루프: github 페이지 초안 재측정, 도메인별 2~3회 판정
6. 브라우저 서비스 화면을 폰 화면 스트림(:99)에 띄우는 설계

## 원본 기록 위치
- 세션 로그: `belmont-browse/.state/logs/` (재시작 스크립트가 여기 씀)
- 침묵 종료 부검 근거: 커밋 `212c731` 메시지, `tests/silent-tail-detector.test.mjs`
- 지식 저장소: `sand-data/knowledge/{sites,rules,lessons,drafts}`

## 저녁 추가분 (PWA 전 기능 실제 검토 + 개선)
- 검토 결과와 개선 내역: `docs/pwa-review-2026-09-03.md` (증거 화면 `docs/pwa-review-2026-09-03/`)
- Belmont 호스트 **미커밋** 변경: 첨부 원래 이름을 봇 프롬프트에 전달 (`system-prompt.ts`, `prompt-collector-glue.ts`, `host-runner-composition.ts`, `send-turn-dispatch.ts`, `send-pipeline.ts`, 테스트 `tests/attached-files-note.test.mjs`). 런타임 재빌드·재시작 완료.
- PWA(`grok-mobile-belmont-pwa/`, git 밖) 수정: 스레드 접기/답글 개수/실시간 갱신, 멤버 편집 유실 방지, 알림 설정 409 안내, 컴퓨터 맞춤/원본 전환, 유령 봇 숨김, 마크다운 렌더, 승인 카드 줄바꿈, 그룹 멘션. `npm run check` 23/23.
- 남긴 테스트 데이터: "모바일 테스트 봇", "모바일 테스트 그룹"(멤버 Scribe+Clerk). 데스크톱에서 삭제 필요.
- 알려진 기존 실패: `tests/codex-native-tool-routing.test.mjs` 3건 (이번 변경과 무관).
- 커밋 `4f27eee`(첨부 이름) 이어 `display-manager.ts` 커밋(VNC 감시·입양·-xdamage·커서) 푸시됨. 세부: `docs/pwa-review-2026-09-03.md` 끝부분.
- 주의: 다른 Claude 세션이 `grok-mobile-belmont-pwa/`를 동시에 편집 중이었음(드래그 임계값 24px 코드). 한 세션만 이 폴더를 만지게 할 것.
- 브라우저 서비스는 이제 `:99`에 Chrome을 띄움(`.state/scripts/restart-browse.sh`). 남은 과제 6번 완료. 되돌리려면 `BELMONT_BROWSE_DISPLAY=:0 .state/scripts/restart-browse.sh`.
- PWA 컴퓨터 화면: 트랙패드 기본, 도구 줄(키보드·Esc·Tab·붙여넣기·복사·이동·맞춤/원본·전체 화면·다시 연결), noVNC 사이드바 숨김. 호스트 커밋 `26a53d0`(VNC 감시·-xdamage).
- 봇별 가상 화면 도입(호스트, 커밋 예정): 배정표 `sand-data/local-displays.json`, 브라우저 봇 `:99`, 나머지 `:100`부터. 세부는 `pwa-review-2026-09-03.md` 끝부분. Xvfb `-noreset`은 새로 뜨는 화면부터 적용(기존 :99/:100/:101은 재시작 전까지 x11vnc 재접속 시 관리자가 바탕·커서를 다시 칠함).
- 2026-09-04: 원본 Aside 확장 프로그램을 포크 없이 봇 브라우저(Chrome for Testing 151)에 올려 데몬(21420)과 연결, 사이드패널 작업 완주. 세부와 되돌리기: `docs/aside-extension-revival.md`. 브라우저 서비스 재시작은 `.state/scripts/restart-browse.sh`.
- GPT Pro 크로미움 복원 패키지 검증: `docs/aside-chromium-r2-review.md` (증거는 정확, 구현은 뼈대).
