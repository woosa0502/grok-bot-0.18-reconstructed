--- a/chrome/browser/ui/views/frame/aside_task_views.cc
+++ b/chrome/browser/ui/views/frame/aside_task_views.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/functional/bind.h"
+#include "base/task/sequenced_task_runner.h"
 #include "base/timer/timer.h"
 #include "base/functional/callback_helpers.h"
 #include "chrome/browser/ui/browser.h"
@@ -104,16 +105,38 @@
     widget_->Show();
   }
 
+  ~PopoverController() = default;
+
  private:
   void OnAction(const std::string& action_id) {
-    on_action_.Run(action_id);
-    if (widget_) {
-      widget_->Close();
+    if (closing_) {
+      return;
+    }
+    // Closing or dispatching an action can remove the anchor/browser. Do not
+    // access controller state after running the external action callback.
+    auto on_action = on_action_;
+    const std::string selected_action = action_id;
+    widget_->Close();
+    on_action.Run(selected_action);
+  }
+  void OnClosed(views::Widget::ClosedReason) {
+    // Close can reenter while Hide() notifies native activation observers.
+    // Keep handling it here until deferred destruction actually runs.
+    widget_->MakeCloseSynchronous(base::BindOnce(
+        &PopoverController::OnClosed, base::Unretained(this)));
+    if (closing_) {
+      return;
     }
+    closing_ = true;
+    // The bubble activation observer is still using its delegate after this
+    // callback returns. Keep both widget and delegate alive until that stack
+    // unwinds, including nested native event loops (DeleteSoon is non-nestable).
+    base::SequencedTaskRunner::GetCurrentDefault()->DeleteSoon(FROM_HERE, this);
+    widget_->Hide();
   }
-  void OnClosed(views::Widget::ClosedReason) { delete this; }
 
   base::RepeatingCallback<void(const std::string&)> on_action_;
+  bool closing_ = false;
   std::unique_ptr<views::BubbleDialogDelegate> delegate_;
   std::unique_ptr<views::Widget> widget_;
 };
@@ -253,7 +276,7 @@
 
 namespace {
 
-// Self-owned: deletes itself when its widget closes.
+// Self-owned: destroys itself after the widget close callback has unwound.
 class ToastController {
  public:
   ToastController(views::View* anchor, const std::u16string& text) {
@@ -270,19 +293,39 @@
         delegate_.get(),
         base::BindOnce(&ToastController::OnClosed, base::Unretained(this)));
     widget_->Show();
-    timer_.Start(FROM_HERE, base::Seconds(2),
-                 base::BindOnce(&ToastController::Dismiss,
-                                base::Unretained(this)));
+    if (!closing_) {
+      timer_.Start(FROM_HERE, base::Seconds(2),
+                   base::BindOnce(&ToastController::Dismiss,
+                                  base::Unretained(this)));
+    }
   }
 
+  ~ToastController() = default;
+
  private:
   void Dismiss() {
-    if (widget_) {
+    if (!closing_) {
       widget_->Close();
     }
   }
-  void OnClosed(views::Widget::ClosedReason) { delete this; }
+  void OnClosed(views::Widget::ClosedReason) {
+    // Close can reenter while Hide() notifies native activation observers.
+    // Keep handling it here until deferred destruction actually runs.
+    widget_->MakeCloseSynchronous(base::BindOnce(
+        &ToastController::OnClosed, base::Unretained(this)));
+    if (closing_) {
+      return;
+    }
+    closing_ = true;
+    timer_.Stop();
+    // The bubble activation observer is still using its delegate after this
+    // callback returns. Keep both widget and delegate alive until that stack
+    // unwinds, including nested native event loops (DeleteSoon is non-nestable).
+    base::SequencedTaskRunner::GetCurrentDefault()->DeleteSoon(FROM_HERE, this);
+    widget_->Hide();
+  }
 
+  bool closing_ = false;
   std::unique_ptr<views::BubbleDialogDelegate> delegate_;
   std::unique_ptr<views::Widget> widget_;
   base::OneShotTimer timer_;
@@ -301,7 +344,7 @@
     views::View* anchor,
     const base::DictValue& popover,
     base::RepeatingCallback<void(const std::string&)> on_action) {
-  if (!anchor) {
+  if (!anchor || !anchor->GetWidget()) {
     return;
   }
   new PopoverController(anchor, popover, std::move(on_action));  // self-owned
--- a/chrome/browser/ui/views/frame/aside_tab_search_bubble.cc
+++ b/chrome/browser/ui/views/frame/aside_tab_search_bubble.cc
@@ -14,6 +14,7 @@
 #include "ui/gfx/geometry/insets.h"
 #include "ui/gfx/geometry/size.h"
 #include "base/functional/bind.h"
+#include "base/task/sequenced_task_runner.h"
 #include "ui/views/bubble/bubble_border.h"
 #include "ui/views/bubble/bubble_dialog_delegate_view.h"
 #include "ui/views/layout/fill_layout.h"
@@ -59,9 +60,11 @@
 // outlive the widget, so it is declared first (destroyed last).
 class AsideTabSearchBubbleController {
  public:
-  AsideTabSearchBubbleController(views::View* anchor, Profile* profile) {
+  AsideTabSearchBubbleController(views::View* anchor,
+                                 Profile* profile,
+                                 views::BubbleBorder::Arrow arrow) {
     delegate_ = std::make_unique<views::BubbleDialogDelegate>(
-        anchor, views::BubbleBorder::TOP_LEFT);
+        anchor, arrow);
     delegate_->SetButtons(static_cast<int>(ui::mojom::DialogButton::kNone));
     delegate_->SetShowCloseButton(false);
     delegate_->SetShowTitle(false);
@@ -76,9 +79,26 @@
     widget_->Show();
   }
 
+  ~AsideTabSearchBubbleController() = default;
+
  private:
-  void OnClosed(views::Widget::ClosedReason) { delete this; }
+  void OnClosed(views::Widget::ClosedReason) {
+    // Close can reenter while Hide() notifies native activation observers.
+    // Keep handling it here until deferred destruction actually runs.
+    widget_->MakeCloseSynchronous(base::BindOnce(
+        &AsideTabSearchBubbleController::OnClosed, base::Unretained(this)));
+    if (closing_) {
+      return;
+    }
+    closing_ = true;
+    // The bubble activation observer is still using its delegate after this
+    // callback returns. Keep both widget and delegate alive until that stack
+    // unwinds, including nested native event loops (DeleteSoon is non-nestable).
+    base::SequencedTaskRunner::GetCurrentDefault()->DeleteSoon(FROM_HERE, this);
+    widget_->Hide();
+  }
 
+  bool closing_ = false;
   std::unique_ptr<views::BubbleDialogDelegate> delegate_;
   std::unique_ptr<views::Widget> widget_;
 };
@@ -86,9 +106,11 @@
 }  // namespace
 
 // static
-bool AsideTabSearchBubbleContentsView::MaybeShow(views::View* anchor,
-                                                 Profile* profile) {
-  if (!anchor || !profile) {
+bool AsideTabSearchBubbleContentsView::MaybeShow(
+    views::View* anchor,
+    Profile* profile,
+    views::BubbleBorder::Arrow arrow) {
+  if (!anchor || !anchor->GetWidget() || !profile) {
     return false;
   }
   extensions::ExtensionRegistry* registry =
@@ -96,7 +118,7 @@
   if (!registry || !registry->enabled_extensions().GetByID(kAsideExtensionId)) {
     return false;
   }
-  new AsideTabSearchBubbleController(anchor, profile);  // self-deleting
+  new AsideTabSearchBubbleController(anchor, profile, arrow);  // self-deleting
   return true;
 }
 
--- a/chrome/browser/ui/views/profiles/aside_profile_menu_view.cc
+++ b/chrome/browser/ui/views/profiles/aside_profile_menu_view.cc
@@ -12,6 +12,7 @@
 
 #include "base/files/file_path.h"
 #include "base/functional/bind.h"
+#include "base/task/sequenced_task_runner.h"
 #include "base/logging.h"
 #include "base/strings/strcat.h"
 #include "base/strings/utf_string_conversions.h"
@@ -398,9 +399,26 @@
     widget_->Show();
   }
 
+  ~AsideProfileMenuController() = default;
+
  private:
-  void OnClosed(views::Widget::ClosedReason) { delete this; }
+  void OnClosed(views::Widget::ClosedReason) {
+    // Close can reenter while Hide() notifies native activation observers.
+    // Keep handling it here until deferred destruction actually runs.
+    widget_->MakeCloseSynchronous(base::BindOnce(
+        &AsideProfileMenuController::OnClosed, base::Unretained(this)));
+    if (closing_) {
+      return;
+    }
+    closing_ = true;
+    // The bubble activation observer is still using its delegate after this
+    // callback returns. Keep both widget and delegate alive until that stack
+    // unwinds, including nested native event loops (DeleteSoon is non-nestable).
+    base::SequencedTaskRunner::GetCurrentDefault()->DeleteSoon(FROM_HERE, this);
+    widget_->Hide();
+  }
 
+  bool closing_ = false;
   std::unique_ptr<views::BubbleDialogDelegate> delegate_;
   std::unique_ptr<views::Widget> widget_;
 };
--- a/chrome/browser/ui/views/tabs/aside_agent_tabs_button.cc
+++ b/chrome/browser/ui/views/tabs/aside_agent_tabs_button.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/functional/bind.h"
+#include "base/task/sequenced_task_runner.h"
 #include "base/strings/string_number_conversions.h"
 #include "chrome/browser/ui/aside/aside_agent_tabs.h"
 #include "chrome/browser/ui/aside/aside_extension_bridge.h"
@@ -126,9 +127,26 @@
     widget_->Show();
   }
 
+  ~AgentTabsBubbleController() = default;
+
  private:
-  void OnClosed(views::Widget::ClosedReason) { delete this; }
+  void OnClosed(views::Widget::ClosedReason) {
+    // Close can reenter while Hide() notifies native activation observers.
+    // Keep handling it here until deferred destruction actually runs.
+    widget_->MakeCloseSynchronous(base::BindOnce(
+        &AgentTabsBubbleController::OnClosed, base::Unretained(this)));
+    if (closing_) {
+      return;
+    }
+    closing_ = true;
+    // The bubble activation observer is still using its delegate after this
+    // callback returns. Keep both widget and delegate alive until that stack
+    // unwinds, including nested native event loops (DeleteSoon is non-nestable).
+    base::SequencedTaskRunner::GetCurrentDefault()->DeleteSoon(FROM_HERE, this);
+    widget_->Hide();
+  }
 
+  bool closing_ = false;
   std::unique_ptr<views::BubbleDialogDelegate> delegate_;
   std::unique_ptr<views::Widget> widget_;
 };
@@ -156,9 +174,7 @@
 }
 
 AsideAgentTabsButton::~AsideAgentTabsButton() {
-  if (browser_) {
-    browser_->tab_strip_model()->RemoveObserver(this);
-  }
+  TabStripModelObserver::StopObservingAll(this);
 }
 
 void AsideAgentTabsButton::UpdateState() {
@@ -171,6 +187,13 @@
   }
 }
 
+void AsideAgentTabsButton::OnTabStripModelDestroyed(
+    TabStripModel* tab_strip_model) {
+  // A native parent window may close before deferred bubble destruction.
+  browser_ = nullptr;
+  SetEnabled(false);
+}
+
 void AsideAgentTabsButton::OnTabStripModelChanged(
     TabStripModel* tab_strip_model,
     const TabStripModelChange& change,
@@ -244,14 +267,12 @@
 }
 
 AsideAgentTabsBubbleView::~AsideAgentTabsBubbleView() {
-  if (browser_) {
-    browser_->tab_strip_model()->RemoveObserver(this);
-  }
+  TabStripModelObserver::StopObservingAll(this);
 }
 
 // static
 void AsideAgentTabsBubbleView::Show(views::View* anchor, Browser* browser) {
-  if (!anchor || !browser) {
+  if (!anchor || !anchor->GetWidget() || !browser) {
     return;
   }
   new AgentTabsBubbleController(anchor, browser);  // self-deleting
@@ -259,6 +280,9 @@
 
 void AsideAgentTabsBubbleView::RebuildRows() {
   rows_->RemoveAllChildViews();
+  if (!browser_) {
+    return;
+  }
   for (int index : aside::GetAgentTabIndices(browser_->tab_strip_model())) {
     rows_->AddChildView(std::make_unique<AgentTabsRowView>(browser_, index));
   }
@@ -269,6 +293,14 @@
   }
 }
 
+void AsideAgentTabsBubbleView::OnTabStripModelDestroyed(
+    TabStripModel* tab_strip_model) {
+  // A native parent window may close before deferred bubble destruction.
+  browser_ = nullptr;
+  rows_->RemoveAllChildViews();
+  SetEnabled(false);
+}
+
 void AsideAgentTabsBubbleView::OnTabStripModelChanged(
     TabStripModel* tab_strip_model,
     const TabStripModelChange& change,
@@ -286,10 +318,15 @@
 }
 
 void AsideAgentTabsBubbleView::OnCloseAllPressed() {
-  aside::CloseAllAgentTabs(browser_->tab_strip_model());
+  if (browser_) {
+    aside::CloseAllAgentTabs(browser_->tab_strip_model());
+  }
 }
 
 void AsideAgentTabsBubbleView::OnConfigureAutoClosePressed() {
+  if (!browser_) {
+    return;
+  }
   // RE: the button file builds "/newtab.html#/u/<uid>" + "/settings/general".
   aside::ShowExtensionPage(
       browser_, aside::ExtensionPageUrl(browser_->profile(), "newtab.html",
--- a/chrome/browser/ui/views/frame/aside_vertical_tab_strip_additions.cc
+++ b/chrome/browser/ui/views/frame/aside_vertical_tab_strip_additions.cc
@@ -9,6 +9,7 @@
 #include <utility>
 
 #include "base/functional/bind.h"
+#include "base/feature_list.h"
 #include "base/json/json_reader.h"
 #include "base/strings/utf_string_conversions.h"
 #include "chrome/browser/profiles/profile.h"
@@ -69,6 +70,12 @@
 
 namespace {
 
+// The observed original puts attention-requiring sessions in Chats. Preserve
+// the reconstructed separate section as an opt-in for diagnostics/parity work.
+BASE_FEATURE(kAsideTaskAttentionSection,
+             "AsideTaskAttentionSection",
+             base::FEATURE_DISABLED_BY_DEFAULT);
+
 // Original strings (Aside 1.0.825.1 locale pak; ids in comments).
 constexpr char16_t kViewOrganizer[] = u"View Organizer";   // IDS 11728
 constexpr char16_t kHideOrganizer[] = u"Hide Organizer";   // IDS 11729
@@ -499,6 +506,8 @@
   int shown = 0;
   int total = 0;
   bool attention = false;
+  const bool show_attention_section =
+      base::FeatureList::IsEnabled(kAsideTaskAttentionSection);
   for (const base::Value& entry : parsed->GetList()) {
     const base::DictValue* item = entry.GetIfDict();
     if (!item || !item->FindString("id") || !item->FindString("title")) {
@@ -508,7 +517,7 @@
     const bool needs_attention =
         status && (*status == "running" || *status == "awaiting-approval" ||
                    *status == "awaiting-answer" || *status == "error");
-    if (needs_attention) {
+    if (show_attention_section && needs_attention) {
       views::View* row = MakeTaskRow(tasks_rows_, *item);
       attention = true;
       MaybeShowPopover(row, *item);
--- a/chrome/browser/ui/views/tabs/aside_agent_tabs_button.h
+++ b/chrome/browser/ui/views/tabs/aside_agent_tabs_button.h
@@ -32,6 +32,7 @@
   void UpdateState();
 
   // TabStripModelObserver:
+  void OnTabStripModelDestroyed(TabStripModel* tab_strip_model) override;
   void OnTabStripModelChanged(
       TabStripModel* tab_strip_model,
       const TabStripModelChange& change,
@@ -65,6 +66,7 @@
   static void Show(views::View* anchor, Browser* browser);
 
   // TabStripModelObserver:
+  void OnTabStripModelDestroyed(TabStripModel* tab_strip_model) override;
   void OnTabStripModelChanged(
       TabStripModel* tab_strip_model,
       const TabStripModelChange& change,
--- a/chrome/browser/ui/views/frame/aside_tab_search_bubble.h
+++ b/chrome/browser/ui/views/frame/aside_tab_search_bubble.h
@@ -7,6 +7,7 @@
 
 #include "base/memory/raw_ptr.h"
 #include "ui/base/metadata/metadata_header_macros.h"
+#include "ui/views/bubble/bubble_border.h"
 #include "ui/views/controls/webview/webview.h"
 #include "ui/views/view.h"
 
@@ -34,7 +35,10 @@
 
   // Shows the bubble anchored at |anchor| if the Aside extension is installed;
   // returns false when the caller should fall back to Chromium's tab search.
-  static bool MaybeShow(views::View* anchor, Profile* profile);
+  static bool MaybeShow(
+      views::View* anchor,
+      Profile* profile,
+      views::BubbleBorder::Arrow arrow = views::BubbleBorder::TOP_LEFT);
 
   views::View* web_view() { return web_view_; }
 
--- a/chrome/browser/ui/views/frame/aside_profile_footer_view.cc
+++ b/chrome/browser/ui/views/frame/aside_profile_footer_view.cc
@@ -135,8 +135,10 @@
 }
 
 void AsideProfileFooterView::OnSearchPressed() {
-  AsideTabSearchBubbleContentsView::MaybeShow(search_button_,
-                                              browser_->profile());
+  // Linux does not automatically flip an off-screen bubble. The footer must
+  // open upward; header callers keep the default downward arrow.
+  AsideTabSearchBubbleContentsView::MaybeShow(
+      search_button_, browser_->profile(), views::BubbleBorder::BOTTOM_LEFT);
 }
 
 BEGIN_METADATA(AsideProfileFooterView)
