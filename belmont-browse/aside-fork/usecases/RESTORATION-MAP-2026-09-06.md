# 복원 지도 — 우리 포크가 원본 Aside 대비 어디까지 왔나 (2026-09-06)

> **최종 갱신 17:22.** 아침 판(09:30)은 "무엇이 남았나"의 지도였다. 이 판은 **오늘 복원 작업
> (G1~G7 + HEAL)이 끝난 뒤의 최종 상태**다. 76군데의 "우리 상태" 열을 각 그룹 결과 문서의 판정으로
> 바꾸고 "증거" 열을 붙였다. 새 측정은 없다. 숫자는 원자료 `patch-map.json`의 `unmatched_bytes`로
> 다시 세었다.

작성 모델: **Claude Opus 5 (1M context)** (모델 ID `claude-opus-5[1m]`).
문서만 씀. 코드·빌드·실행 중인 서비스는 건드리지 않았다.

---

## 1. 결론 먼저

**조밀 구간 76군데 중 64군데가 닫혔다. 코드 크기로 94.43%다.**
아침에는 13군데(19.20%)만 재현돼 있었다. 오늘 51군데(75.23%)를 더 닫았다.

남은 것은 두 종류뿐이다. **불가 9군데(2.97%)** — 맥·윈도 전용이라 리눅스에 물건이 없는 것 5군데,
정체를 못 밝힌 것 4군데. 그리고 **필요 없음 3군데(2.60%)** — 상위 크로미움 기능이거나 맥 전용이라
처음부터 만들 것이 없던 자리.

| 상태 | 구간 수 | 바이트 | 비율 |
|---|---|---|---|
| **재현 완료** | 53 | 2,919,200 | **89.31%** |
| **원본과 차이 없음 확인** | 9 | 151,632 | 4.64% |
| **상위에 이미 있음** | 2 | 15,872 | 0.49% |
| 불가 (맥·윈 전용) | 5 | 72,272 | 2.21% |
| 불가 (정체 미상) | 4 | 24,752 | 0.76% |
| 필요 없음 | 3 | 84,976 | 2.60% |
| **합계** | **76** | **3,268,704** | **100%** |

닫힌 것 = 재현 완료 + 차이 없음 + 상위에 있음 = **64군데 / 3,086,704 B / 94.43%**.

### 오늘 하루의 차이

| 시점 | 닫힌 구간 | 바이트 | 비율 |
|---|---|---|---|
| 아침 (09:30) | 13 | 627,712 | 19.20% |
| 오늘 닫음 | +51 | +2,458,992 | +75.23% |
| **저녁 (17:22)** | **64** | **3,086,704** | **94.43%** |

### 세 층 요약

| 층 | 아침 | 저녁 |
|---|---|---|
| 데몬 · 내장 확장 | 원본 그대로 재사용, 남은 일 없음 | 그대로 |
| 확장 비공개 API (함수 77 + 이벤트 20) | 표면 완성, 계약 62/62 | **속까지** — 원본 함수 63개 문자열 대조, 빠진 8군데 채움 (G6) |
| 크로미움 안 코드 (76군데 / 3,268,704 B) | 35군데 손댐, 41군데 미착수 | **64군데 닫음, 12군데 남음(불가 9 + 필요 없음 3)** |

주의는 아침 판과 같다. 바이트는 그 구간의 **안 맞는 블록 바이트**이지 구간 전체 크기가 아니다.
이 지도는 x86_64 슬라이스 기준이라 우리 RE 문서의 arm64 주소와 숫자가 다르다.

---

## 2. 본 표 — 조밀 구간 76군데 전부

상태는 여섯으로 묶고, 묶음 안은 크기 큰 순이다.
"담당"은 오늘 그 구간을 맡은 그룹이다. `기존`은 아침에 이미 재현돼 있어 배정하지 않은 자리다.

### 재현 완료 — 53군데 / 2,919,200 바이트 (89.31%)

| # | 기능 | 크기(B) | 담당 | 증거 |
|---|---|---|---|---|
| 1 | 세로탭 스트립 — 작업/채팅 행, 채팅 이름 바꾸기 `0x04cab230` | 225,136 | G1 | G1-RESULT §2-1·§7. 캡처 `G1-23`~`G1-31`. 채팅 행 메뉴 7항목·명령 번호까지 원본대로. 가짜 데몬(8712)으로 이름 바꾸기·보관 왕복 실측. 발치 바닥 고정은 G7 §4 (`G7-01`, `G7-02`) |
| 2 | 광고 차단 서비스 `0x04815ff0` | 212,992 | 기존 | DIFFERENTIAL (7)(8) A1~A7, patch 035~040. G4가 종단 검사 **29/29** 재확인 (`test/g4/adblock/result-G4-2026-09-06.json`) |
| 3 | 세로탭 북마크 메뉴·폴더 편집 대화상자 / AiTabs 뷰포트 / 상단 로딩바 `0x04c7cce0` | 185,600 | G1 (+G2 이관) | G1-RESULT §2-2. 캡처 `G1-00`, `G1-03`~`G1-07`, `G1-22`. 대화상자 4개 문구 원본 그대로. 분할 보기 조각 2개는 G2가 만듦(G2-RESULT §3-4·이관 B) |
| 4 | 가져오기 — 사파리·파이어폭스·크로미움 프로필 옮기기 `0x048c1ab0` | 173,504 | G4 | G4-RESULT §3(1). `G4-import-webui.png`. `aside_importer_tests` **9/9**, `aside_native_contracts_tests` 5/5. 실제 데이터 이관은 픽스처까지 → **L7** |
| 5 | 알림 권한 프롬프트 문구 · 세로탭 섹션 간 드래그 미리보기 `0x04d0c8f0` | 163,712 | G1 | G1-RESULT §2-3. 캡처 `G1-16`(알림 문구), `G1-18`(한 번 허용 문구). 드래그 미리보기는 코드 확인만 |
| 6 | 확장 API 구현 (browserImport / omnibox / browserPreferences) `0x048f2710` | 147,040 | G6 | G6-RESULT §2. 원본 함수 **63/63 존재**, 문자열 대조로 빠진 8군데 채움 (`cursorPosition`, `areMatchesShowing`, `onNavigationLikely` 실구현 등). 아침의 "겉만"이 해소됨 |
| 7 | page_info 재적재 버블 · 탭 펼침 셰브런 · AI 탭 개수 배지 `0x04899120` | 136,656 | G2 | G2-RESULT §3-1. `G2-07-page-info-reload-bubble.png`. 원본에 `PageInfoInfoBarDelegate` 문자열 0개인 것이 결정적 근거. 개수 배지는 → **L1** |
| 8 | sync 프로토콜 엔티티 (AsideWebsiteStorage) `0x0a6f77e0` | 113,680 | G5 | G5-RESULT §4·§7가. `G5-01-sync-internals-encrypted-types.png`. 히스토그램 64바이트 점프표가 원본과 **바이트 일치** |
| 9 | 미니팝업 좌표기 · 탭 검색 버블 `0x04c12090` | 100,560 | 기존 | HANDOFF B1·B2, patch 011/012/039, UI-PLAN 증분 2 |
| 10 | Aside 프로필 메뉴 본문 · 아이콘 선택기 `0x04bd8b00` | 82,384 | G3 | G3-RESULT §1·2·8. 캡처 `G3-01`~`G3-04`. 원본 문구 12개 주소까지 대조, 지시 아이콘 **47개 이름·순서 일치** |
| 11 | 프로필 메뉴 뷰 · 이니셜 아바타 `0x04bf8a90` | 80,368 | G3 | G3-RESULT §1·2·8. `G3-04`(Rocket 고르면 아바타가 바뀜), `Local State`에 `aside_profile_indicator_icon` 저장 확인 |
| 12 | 콘텐츠 컨테이너 — 포커스·모달 처리 `0x04c52470` | 76,432 | G2 | G2-RESULT §3-2. `PostFocusAfterActivation`·`kIsBlockedByModalKey` 이름·조건 일치 (코드 확인) |
| 13 | 컴포넌트 업데이터 — 데몬 배포·기동·강제종료 `0x0491e180` | 72,560 | G5 | G5-RESULT §1·§7라. `G5-02-installed-aside-components.png`. **진짜 서명 CRX 두 개**를 끝까지 설치. Aside publisher 키·비밀번호 관리자 키 회수 |
| 14 | 데몬 인증 · 동기화 암호화 키 대기 상태기계 `0x04768830` | 71,728 | G5 | G5-RESULT §6·§7마. `G5-logs/replay-requests-final.log`에 challenge→session→Authorization 실측. 계정 왕복은 미확인 |
| 15 | 분할 보기 장식 (그림자, 세로탭 인접 페인) `0x04c40660` | 64,800 | G2 | G2-RESULT §3-3. `G2-04-split-pane-shadow.png`(250% 확대). 레이어 이름 `Split Pane Shadow` 원본과 같음 |
| 16 | 보안 원격 디버깅 자격증명 · pq_v1 `0x043eda40` | 64,528 | G4 | G4-RESULT §3(2). 악수 **17/17** (`test/g4/secure-cdp-handshake.py`), 설치 메타 **7/7** (`secure-cdp-metadata.sh`). pq_v1 서명 알맹이만 불가 → **X1** |
| 17 | 분할 보기 본체 (MultiContentsView 통합) `0x04c6d1f0` | 62,624 | G2 | G2-RESULT §3-4. `G2-04-splitview.png`. `AsideAiTabsViewport` 이름·params·순서 원본대로 → 동작은 **L2** |
| 18 | 탭 전환기 (Ctrl+Tab) `0x04c31280` | 60,880 | 기존 (+G7 색) | UI-PLAN 증분 4. G7 §3이 두 색을 원본 이름표로 교체(등급 A). 화면 확인은 → **L10** |
| 19 | Geist 폰트 등록 · headless 판정 · AppleScript 북마크 `0x047c9720` | 60,064 | G3 | G3-RESULT §3. `fc-match "Geist"` → `Geist-Regular.ttf`. 세 갈래 중 Aside 것은 Geist뿐, 나머지 둘은 상위와 같음을 기준선 대조로 확정 |
| 20 | 프로필 페이저 (창 안에서 탭 보관·복원) `0x04bc7560` | 57,696 | G3 | G3-RESULT §4. 캡처 `G3-06a`·`G3-06`·`G3-07`. **치명 결함 4건**을 찾아 고침. 창 번호(`session_id`)가 같음을 CDP로 확인 |
| 21 | 주소창 개조 — AI 모드, 페이지 로드 진행 표시 `0x047ed7c0` | 56,784 | G2 | G2-RESULT §3-5. `G2-05-omnibox-load-progress.png`. 채움 끝 x 좌표를 프레임별로 실측. 타이머 500 ms는 원본 역어셈블과 일치 |
| 22 | 계정 비밀번호 게이트 `0x047fea70` | 40,736 | 기존 | HANDOFF B5, patch 039, `test/password-gate/run-gate-test.sh` |
| 23 | 탭 우클릭 메뉴 재구성 `0x04863470` | 40,640 | G1 | G1-RESULT §2-4. 캡처 `G1-08`~`G1-10`. 빠져 있던 지표 하나(`Tab.ContextMenu.AddToBookmarks.SelectedTabsCount`)와 토글 동작 |
| 24 | sync 엔진 — 데이터 타입 구성·암호화 타입 변경 `0x04a2a8d0` | 37,616 | G5 | G5-RESULT §8 (파생). 구간 문자열·소스 경로가 100% 상위 코드. 타입을 끼워 넣으면 자동으로 같아짐 |
| 25 | sync 필드 — 결제·작업 맥락·웹사이트 저장소 `0x04a20f90` | 34,976 | G5 | G5-RESULT §7다. 빌드된 바이너리에 `aside_website_storage` 12곳 등 문자열 8종 확인 |
| 26 | 에이전트 탭 버튼·버블 · 탭 그룹 편집 메뉴 `0x04d3ea40` | 34,512 | 기존 | UI-PLAN 증분 3 |
| 27 | 세로탭 섹션 뷰 (북마크 섹션 머리) `0x04ce90e0` | 32,688 | 기존 (+G7) | UI-PLAN 증분 1·5. G7 §6-1이 프로필 전환 재결속 추가, §3이 글자색 2자리를 원본 이름표로(등급 B) |
| 28 | 계정 등록부 읽기 (accounts.json) `0x047ae6a0` | 29,392 | 기존 | DIFFERENTIAL (4) |
| 29 | 세로탭 작업 섹션 — 채팅 보관, 상태 문구 `0x04cf7e80` | 29,072 | G1 (+G7 색) | G1-RESULT §2-5. 캡처 `G1-23`, `G1-28`~`G1-30`. 보관 대화상자 2개 문구 원본대로. G7이 상태 문구 색을 `kColorAsideMutedForeground`로 |
| 30 | 프로필 속성 갱신기 · 데몬 인가 `0x047e2b10` | 28,080 | 기존 | DIFFERENTIAL (4) |
| 31 | 에이전트 탭 상태 버블 · `aside://` 스킴 처리 `0x04d36f80` | 25,056 | G1 | G1-RESULT §2-6 + §8가. `G1-33`. 확인 중 **실제 결함 발견**: `ExpandBubble()`이 치환을 되돌리던 것을 고침 |
| 32 | 가져오기 WebUI 자원 · 첫 소개 화면 `0x0488bd30` | 24,528 | G4 | G4-RESULT §3(3). `G4-settings-import.png`. 원본 pak 16130–16141을 **바이트 그대로**. 빠져 있던 문자열 7789 복원 |
| 33 | 주소창 제안 UI (searchbox mojom) `0x0474d900` | 23,296 | G2→G6 | G6-RESULT §5(14). G2는 불가로 닫았으나 G6이 속을 채움 — 빈 Page 메서드 **16개**를 스키마의 이벤트로 연결 (§7 문서 충돌 1) |
| 34 | 세로탭 작업 배지·스로버·상태점 `0x04cff670` | 22,544 | 기존 (+G7 색) | UI-PLAN 증분 6. G7 §3이 상태점 4색을 원본 이름표로(등급 B) |
| 35 | 화면 공유할 때 뜨는 탭 고르기 목록 `0x04892a00` | 21,568 | G3 (+G7 색) | G3-RESULT §6. `G3-11`, `G3-12`. `BorderOnTopView`를 `PaintChildren` 기계까지 이식. G7 §3이 색 3자리를 원본 이름표로(등급 A) — `G7-11`, `G7-12` |
| 36 | 고정 탭 서비스 — 탐색 가로채기·재조정 `0x0485d300` | 21,328 | G1 | G1-RESULT §2-8. `G1-20`(같은 사이트), `G1-21`(다른 사이트 → 새 탭). 판정 규칙은 추정이라고 그대로 남김 |
| 37 | 탭 검색 WebView · 전환기 항목 뷰 `0x04c2ba90` | 20,160 | 기존 | UI-PLAN 증분 2 |
| 38 | 업데이트 배지 버튼 `0x04855d90` | 19,632 | 기존 (+G7 색) | DIFFERENTIAL 증분 7b. G7 §3이 배지 3자리를 원본 이름표로(등급 B), `G7-11`·`G7-12`에 실물 |
| 39 | 가로/세로 고정 탭 칩 앞부분 (정체 미상이었음) `0x04c65910` | 18,336 | G6 | G6-RESULT §3-2. 링크 이웃·호출 대상으로 `horizontal_tab_strip_region_view.cc` 계통 확정. 코드는 이미 `aside_pinned_entries_view.*`에 있음 (`G6-pinned-entries.png`) |
| 40 | 알림 인박스 저장 (SQLite, 암호화 payload) `0x047a84c0` | 16,256 | G3 | G3-RESULT §7. `CREATE TABLE`·`INSERT` 두 문장이 원본과 **글자까지 같음**. 종단 확인은 → **L4** |
| 41 | 컴포넌트 업데이터 파이프라인·압축 해제 `0x04a357a0` | 13,808 | G5 | G5-RESULT §5. 상위 `components/update_client` 그대로. 같은 실행에서 download→crx3 파이프라인이 실제로 돎 |
| 42 | 맥 전역 단축키 경로 → 리눅스 X11 경로 `0x0498f6c0` | 13,760 | G4 | G4-RESULT §3(4). `ui::GlobalAcceleratorListener`로 대체, 실패 문구 3개 원본 그대로. 창 토글은 → **L6** |
| 43 | Aside 메뉴 패널 · 메뉴 항목 버튼 `0x04bf5120` | 13,184 | G3 | G3-RESULT §1·2·8. `G3-01` |
| 44 | WebUI 인터페이스 바인더 목록 `0x0d0013f0` | 12,784 | G6 | G6-RESULT §1-3. 원본 133개 vs 기준선 132개의 차이 하나(`tab_search::mojom::PageHandlerFactory`)를 프레임 바인더 맵에 추가 |
| 45 | 세로탭 새 탭 버튼 `0x048bbdd0` | 12,768 | 기존 | UI-PLAN 증분 1 |
| 46 | 광고 차단 렌더러 옵저버 (요소 숨김 주입) `0x0493aec0` | 12,768 | 기존 (+G4 수정) | DIFFERENTIAL (8) A6, patch 037. **G4가 여기서 실사용 버그를 잡았다** — 주입 시점을 `RunScriptsAtDocumentStart`로 옮김 (G4-RESULT §2) |
| 47 | 웹스토어에서 테마 설치를 막음 `0x07f8e140` | 10,064 | G3 | G3-RESULT §9. 원본 꼬리 점프까지 읽어 문구 3개·결과 코드 4(`kFeatureDisabled`)를 그대로. 실물은 → **L5** |
| 48 | 툴바 "Ask Aside" 액션 · 호버 카드 `0x04875aa0` | 9,456 | G2 | G2-RESULT §3-9. `G2-08-ask-aside-hover.png` |
| 49 | sync 무효화 지표 `0x04bbb1d0` | 8,000 | G5 | G5-RESULT §8 (파생) |
| 50 | 가로 탭 스트립 고정 탭 항목 뷰 `0x04c6a630` | 7,248 | G1→G7 | G7-RESULT §5. 캡처 `G7-04`, `G7-05`. G1이 미해결로 남긴 것을 **원인 규명**해 닫음 — `GetChildrenInZOrder()` 목록에 칩이 없었다 (§7 문서 충돌 2) |
| 51 | sync 엔진 백엔드 제어 타입 `0x0cf48930` | 6,144 | G5 | G5-RESULT §8 (파생) |
| 52 | devtools 다운로드 관리자 대리자 `0x046f6cb0` | 5,200 | G4 | G4-RESULT §3(5). `test/g4/devtools-download.mjs` — 폴더 자동 생성 + 디스크는 GUID / 표시 이름은 `report.csv` |
| 53 | sync 엔진 암호문구 API `0x0cf4aff0` | 3,872 | G5 | G5-RESULT §8 (파생) |

### 원본과 차이 없음 확인 — 9군데 / 151,632 바이트 (4.64%)

우리 트리가 이미 원본과 같다. 블록이 안 맞은 이유는 원본이 그 코드를 훨씬 큰 파일 하나에 넣어
컴파일했기 때문이다(원본 추적 문자열이 `vertical_tab_strip_region_view.cc:15365`를 가리킨다 —
우리 파일은 1,200줄 남짓). **매처가 헛짚는다는 것을 G2가 10번 구간에서 실제로 증명했다.**

| # | 기능 | 크기(B) | 담당 | 증거 |
|---|---|---|---|---|
| 54 | 툴바 재구성 `0x0487a6e0` | 45,856 | G2 | G2-RESULT §4-6. Aside 고유 문자열 **0개**. `ToolbarView::Init` 크기 원본 18,336 B vs 기준선 17,728 B (+3.4%) |
| 55 | 탭 헤더 · 닫기 버튼 · 제목 뷰 `0x04d47880` | 22,064 | G1 | G1-RESULT §3. 이름 8개 전부 우리 트리에 있음 |
| 56 | 세로탭 접기/펴기 지표 · 관리형 구성 `0x0899efc0` | 20,288 | G1 | G1-RESULT §3. 지표 6개 + `OriginManagedConfiguration` 전부 있음 |
| 57 | 활성 탭 전환 경로 (TabStripModel) `0x08715f40` | 19,552 | G1 | G1-RESULT §3. 구간의 유일한 문자열 `TabStripModel::ActivateTabAt`. 게다가 23번에서 이 클래스에 명령을 하나 더 넣어 원본이 밀린 이유도 맞춰짐 |
| 58 | 탭 드래그 — 드래그 목적지 `0x0d272740` | 17,664 | G1 | G1-RESULT §3. `tab_drag_controller.cc` 1953·2285·3097줄 |
| 59 | 고정 툴바 액션 컨테이너 `0x04872550` | 12,416 | G2 | G2-RESULT §4-8. 문자열 `RemoveButton` 하나뿐, 우리 트리 같은 이름 |
| 60 | 탭 그룹 헤더 접기/펴기 지표 `0x0889bbd0` | 6,624 | G1 | G1-RESULT §3. 지표 4개 전부 있음 |
| 61 | 세로탭 hover 펼침의 마우스 속도 계산 `0x0d186f70` | 4,816 | G1 | G1-RESULT §3. **아침 판의 문서 충돌 해소** — VERSION-STEPS가 맞다. 함수·지표·기능 매개변수 9개가 트리에 있고 원본 역어셈블과 구조가 같다 |
| 62 | ToggleButton 변형 `0x0afc71e0` | 2,352 | G2 | G2-RESULT §4-10. 원본·기준선·우리 트리 셋이 완전히 같은 모양. 이 구간의 블록 분류가 `unmatched 16 / matched 6`인 것이 **매처가 헛짚는 증거** |

### 상위에 이미 있음 — 2군데 / 15,872 바이트 (0.49%)

| # | 기능 | 크기(B) | 담당 | 증거 |
|---|---|---|---|---|
| 63 | 확장 메뉴 지표 `0x085de3c0` | 8,704 | G3 | G3-RESULT §10. Aside 고유 문자열 0개, 지표 10개가 원본·기준선·우리 트리 셋 다 같음 |
| 64 | 업데이터 mojo 바인딩 (정체 미상이었음) `0x081c9f90` | 7,168 | G5 | G5-RESULT §10. `chrome/updater/mojom/updater_service.mojom` 생성 바인딩으로 정체 확인. `//chrome/updater/mojom`은 리눅스에서도 컴파일되므로 **이미 우리 바이너리에 있다** |

### 불가 (맥·윈 전용) — 5군데 / 72,272 바이트 (2.21%)

리눅스에 그 물건이 없다. 만들 수 있는 것이 아니다.

| # | 기능 | 크기(B) | 담당 | 왜 불가 |
|---|---|---|---|---|
| 65 | 맥 전용 확장 팝오버 `0x04bbe2f0` | 35,424 | G3 | G3-RESULT §5. `extension_popover_mac.mm`(Objective-C++, `NSPopover`). **하는 일은 리눅스에 이미 있다** — `extension_popup.cc:225`. 사실상 "필요 없음"과 같다 |
| 66 | Aside 자체 업데이터 클라이언트 (맥) `0x081c26d0` | 19,888 | G5 | G5-RESULT §10. AuthorizationCreate·SMJobBless·OpenDirectory·NSXPCConnection. 전부 맥OS 전용 |
| 67 | 업데이터 mojo 프록시 `0x081cc570` | 9,616 | G5 | G5-RESULT §10. `BUILD.gn`이 `enable_updater && (is_mac \|\| is_win)`. 켜면 트리 전체 재빌드, 켜도 리눅스 구현은 `NOTREACHED()` 껍데기 |
| 68 | 업데이터 등록·판 확인 `0x081c06c0` | 4,944 | G5 | G5-RESULT §10. 위와 같은 빌드 게이트 |
| 69 | 업데이터 번들 경로 (at.studio.AsideUpdater) `0x081d0360` | 2,400 | G5 | G5-RESULT §10. Mach 부트스트랩은 리눅스에 없다. 원본 상표 상수는 남겨 뒀다 |

부수로, **pq_v1 설치 서명 알맹이**도 이 부류다(맥OS 26 ML-DSA + 키체인). 다만 그 구간
`0x043eda40` 자체는 악수 전체가 재현돼 "재현 완료"에 있다(위 16번, 아래 §5 X1).

### 불가 (정체 미상) — 4군데 / 24,752 바이트 (0.76%)

기능을 못 만든 것이 아니라 **무슨 기능인지 못 밝힌 것**이다.
막힌 이유는 하나다 — 이름표(문자열)도 RTTI도 없다. 원본은 심볼도 벗겨져 있다
(`llvm-nm --defined-only`가 3개만 낸다).

| # | 구간 | 크기(B) | 담당 | 어디까지 좁혔나 |
|---|---|---|---|---|
| 70 | `0x045dd200` | 10,160 | G6 | G6-RESULT §3-4 / G7 §7. 앞뒤 경로 참조가 양쪽 다 멀고, 이웃 vtable 주인도 흩어진다(웹앱 명령·검색·mojo). **그대로 불가** |
| 71 | `0x049ca150` | 7,728 | G6 | G6-RESULT §3-5 / G7 §7. GPU와 크래시패드 사이의 빈 자리. 이 구간을 가리키는 슬롯이 326개 = 작은 vtable이 잔뜩 모인 자리. **그대로 불가** |
| 72 | `0x047c51f0` (해시맵 인라인) | 3,856 | G6 | G6-RESULT §3-6 / G7 §7. **자동 PiP 계통으로 좁힘.** G6의 "템플릿 인스턴스화 덩어리" 가설은 약해졌다(관찰자 목록을 가진 진짜 클래스가 있다). 이름은 못 얻음 |
| 73 | `0x0d145880` | 3,008 | G6 | G6-RESULT §3-7 / G7 §7. **계통 확정** — `chrome/browser/ui/views/extensions/`의 툴바 버튼 계열(이웃 vtable 주인 207·159자리). "확장 툴바냐 찾기 막대냐"의 애매함은 풀렸다. 이름은 못 얻음 |

다음에 뚫을 유일한 길: 같은 리비전 크로미움을 **맥 x86_64로 직접 빌드**해 오브젝트 단위 배치를
비교한다. 이 트리에서는 맥 빌드를 돌릴 수 없다.

### 필요 없음 — 3군데 / 84,976 바이트 (2.60%)

| # | 기능 | 크기(B) | 왜 |
|---|---|---|---|
| 74 | WebUI 툴바 BrowserControls `0x0479a630` | 46,912 | 크로미움 151 상위 기능. 우리 바탕에 이미 있다 |
| 75 | 맥 창 효과(반투명) 관찰자 · 모달 시트 `0x04a0fb50` | 21,840 | 맥 전용 |
| 76 | 맥 AppController URL 열기 `0x0475f620` | 16,224 | 맥 전용 |

### 상태별 합계 (검산)

`patch-map.json`의 `unmatched_bytes`를 상태별로 다시 더한 값이다.

| 상태 | 구간 수 | 바이트 | 비율 |
|---|---|---|---|
| 재현 완료 | 53 | 2,919,200 | 89.31% |
| 원본과 차이 없음 확인 | 9 | 151,632 | 4.64% |
| 상위에 이미 있음 | 2 | 15,872 | 0.49% |
| 불가 (맥·윈 전용) | 5 | 72,272 | 2.21% |
| 불가 (정체 미상) | 4 | 24,752 | 0.76% |
| 필요 없음 | 3 | 84,976 | 2.60% |
| **합계** | **76** | **3,268,704** | **100%** |

### 그룹별 처리량

| 그룹 | 담당 구간 | 바이트 | 닫은 것 | 남긴 것 |
|---|---|---|---|---|
| G1 세로탭·탭 | 14 | 788,800 | 14/14 | 0 (12번 가로판은 G7이 마무리) |
| G2 프레임·툴바·주소창·분할 보기 | 10 | 490,672 | 10/10 | 0 (7번은 G6이 마무리) |
| G3 프로필·알림·확장 UI | 10 | 385,712 | 9/10 | 불가 1 (맥 전용) |
| G4 광고차단·가져오기·보안 CDP | 5 | 281,520 | 5/5 | 0 |
| G5 sync·업데이터 | 14 | 406,400 | 10/14 | 불가 4 (맥·윈 전용) |
| G6 정체 미상·겉만 | 7 | 202,912 | 3/7 | 불가 4 (정체 미상) |
| **배정 합계** | **60** | **2,556,016** | **51/60** | **불가 9** |

배정 밖 16군데 = 아침에 이미 재현돼 있던 13군데 + 필요 없음 3군데.
G7과 HEAL은 구간을 배정받지 않고 **그룹 밖 잔여**를 맡았다(§4, §6).

---

## 3. 라이브 확인 필요 — 통합 목록 (10건)

전부 **Aside 확장 또는 외부 서비스가 있어야 화면에 나오는 것**이다. 오늘 규칙(COMMON.md)이 확장
로드를 금지했다 — 확장을 올리면 라이브 데몬(21420)에 붙어 버리기 때문이다. 코드 근거와 단위 검증은
끝나 있고, **화면 확인만 남았다.**

아래는 한 장 요약이다. **호스트를 올려 그대로 따라 할 실행서는
`usecases/LIVE-CHECK-2026-09-06.md`에 따로 있다** — 준비 명령, 항목별 확인 명령, 판정 기준,
결과를 어디에 적을지까지.

| # | 항목 | 왜 라이브가 필요한가 | 어느 화면 | 어떤 조작 | 기대 결과 | 담당 |
|---|---|---|---|---|---|---|
| L1 | AI 탭 개수 배지 (`AiTabsCountBadgeView`) | 에이전트 탭 그룹은 Aside 확장이 만든다. `aside::IsAgentTabsGroup()`이 참일 때만 보인다 | 가로/세로 탭 스트립의 탭 그룹 머리 | 확장으로 에이전트 탭 그룹을 만든다 | 그룹 머리에 스파클 아이콘 + 개수 배지 | G2 |
| L2 | AI 탭 뷰포트 (`AsideAiTabsViewport`) | 물릴 에이전트 탭이 있어야 Emulation 명령이 나간다 | (화면 없음 — CDP 로그) | 에이전트 탭이 있는 상태에서 창 크기를 바꾼다 | 그 탭의 DevTools 세션에 `Emulation.setDeviceMetricsOverride`, 없어지면 `clearDeviceMetricsOverride` | G2 |
| L3 | 탭 끌기 미리보기 카드 (`FloatingTabDragView`) | X11 합성 드래그는 탭을 새 창으로 떼어 내 분할 드롭 타깃이 안 겨냥된다 | 세로탭 + 분할 보기 | **진짜 마우스로** 세로탭 행을 잡아 분할 드롭 타깃 위로 끈다 | 파비콘+제목이 든 둥근 카드가 포인터를 따라온다 | G2 |
| L4 | 알림 인박스 종단 | `MaybeDeliverNotificationToAside()`가 `IsExtensionEnabled(profile)`을 요구한다 | 아무 사이트 | 알림 권한 허용 → `new Notification()` | `<프로필>/AsideInbox/inbox.db`의 `inbox_entries`에 행 1개 (암호화 BLOB) | G3 |
| L5 | 웹스토어 테마 차단 대화상자 | `webstorePrivate.beginInstallWithManifest3`은 웹스토어 출처에서만 불린다 | 크롬 웹스토어 | 아무 **테마**의 "Chrome에 추가" | 제목 `Theme installation failed` / 본문 `Aside browser does not support themes.` / 버튼 OK | G3 |
| L6 | 미니팝업 창 실제 토글 | 창을 띄우는 것은 Aside 확장이다 | 데스크톱 전역 | `Alt+Space`(기본 단축키) | 프레임 없는 항상-위 창이 뜨고 다시 누르면 숨는다. 프로필 전환도 되는지 | G4 |
| L7 | 파이어폭스·사파리 실제 데이터 이관 | 이 기계에 Firefox 프로필도 Safari 내보내기 ZIP도 없다 | `chrome://aside-import-data` | 실물 프로필이 있는 기계에서 Firefox 선택 → 가져오기 / Safari 내보내기 ZIP 지정 | 북마크·쿠키가 실제로 옮겨진다 | G4 |
| L8 | 비밀번호 관리자 툴바 버튼 자동 고정 | 그 확장(`clcdgiameigmljcbkkcbjiljinmfkncl`)이 있어야 보인다 | 툴바 | 확장을 처음 설치한 상태로 브라우저 시작 | 버튼이 툴바에 **한 번만** 자동 고정되고, pref `aside_password_manager_toolbar_pin_initialized`가 켜진다 | G6 |
| L9 | 확장 페이지 배경색 측정 | 확장 페이지가 떠야 도는 경로다 | 확장 페이지 탭 | 확장 페이지를 연다 | `contents_web_view.cc:216`의 `ScheduleTargetExtensionBodyBackgroundProbe()` 경로가 돌고 크롬 색이 페이지 `body` 색에 맞춰진다 | G6 |
| L10 | 탭 전환기 새 팔레트 | 전환기를 띄우는 경로가 확장·단축키에 걸려 있다 | 탭 전환기 패널 | 전환기를 띄운다 (Ctrl+Tab은 탭 순환이라 다름) | 패널 배경 `kColorAsideTabSwitcherBackground`(라이트 `0xB8F8F8F8` / 다크 `0xB3171717`), 선택 항목 `…ItemBackgroundFocused` | G7 |

---

## 4. 그룹 밖에서 나온 것들

### 4-1. 옛 버전 짝 비교의 추가 후보 6개 — 전부 닫힘

아침 판 3절의 여섯 항목이다. G6이 전부 처리했다.

| 항목 | 아침 | 최종 | 증거 |
|---|---|---|---|
| 북마크가 열린 탭을 대표하는 구조 | 없음 | **재현 완료** | G6-RESULT §4(8). `G6-bookmark-tab-representation-crop.png` — 열린 탭을 대표하는 북마크 줄만 다른 색 |
| `chrome://settings`의 "Aside Account" 구역 | 없음 | **재현 완료** | G6-RESULT §4(9). `G6-settings-aside-account.png`. 원본 문자열 8개가 그대로 계약 |
| 비밀번호 관리자 툴바 자동 고정 pref | 없음 | **재현 완료** (화면은 L8) | G6-RESULT §4(10). 원본 46바이트 키를 `ExtensionPrefs`가 쓰는 자리까지 대조 |
| 확장 페이지 배경색 측정 | 없음 | **재현 완료** (화면은 L9) | G6-RESULT §4(11). `contents_web_view.cc:216` |
| 툴바 접기 버튼 호버 타이머 | 없음(100 ms 감시만 있었음) | **재현 완료 — 빠진 것을 채움** | G6-RESULT §4(12). **500 ms 종료 타이머가 없었다.** `G6-collapse-hover-0`~`3` 4장으로 유예 시간 관찰 |
| 프로필 지시 아이콘 | 없음 | **재현 완료** | G6-RESULT §4(13). `ProfileIndicatorIconButton`·`…PickerPanel` 둘 다 원본과 1:1. `G3-03`, `G3-04` |

### 4-2. 원본 색 이름표 66개 — 아침 지도에 없던 자리

G3이 화면 공유 선택기를 이식하다 **원본 바이너리에 색 이름표가 통째로 들어 있는 것**을 발견했다
(x86_64 `0x0e60f2d8`, 623개, 배열 순서 = `ui::ColorId`). 그 안에 `kColorAside*`가 214~279번
**66개** 있었고, 우리 트리에는 하나도 없었다. G7이 이름과 값을 전부 되찾았다.

| 항목 | 결과 |
|---|---|
| 이름 | **66/66** — 이름표 배열에서 그대로 |
| 값 (라이트·다크) | **66/66, 미확인 0** — 원본 믹서 함수 `0x035f9240`을 직접 역어셈블 |
| 값의 모양 | 리터럴 SkColor 58개 + 다른 Aside 색의 별칭 8개. **알파 혼합·대비 보정 0개** (전부 항이 하나) |
| 극성 검증 | `kColorAsideTooltipBackground` 라이트 `0xFFFAFAFA` / 다크 `0xFF18181B` — 뒤집혔으면 라이트 툴팁이 검정이 된다 |
| 트리 이식 | `chrome/browser/ui/color/aside_color_mixer.{h,cc}` 신규. 상위 색 번호를 하나도 밀지 않게 크롬 구간 **뒤에** 붙였다 |
| 근사치 교체 | 8개 파일 **16자리**. 증거 등급 A(원본 함수 직접 읽음) / B(원본 구간에 그 색만 있음) / C(이름 대응)를 붙여서만 바꿨다 |
| 안 바꾼 자리 | 5자리 — 근거가 없어 그대로 뒀다 (G7-RESULT §3 아래 표) |

실물 확인: `G7-11-dark-media-picker.png`, `G7-12-light-media-picker.png`
(화면 공유 선택기 판 + 업데이트 배지가 원본 값으로 칠해진 것).

부수로 **상단 로딩바는 색만이 아니라 구조도 틀렸었다** — 원본은 바탕 전체를 칠하고 그 위에 진행분을
칠한다(`FillRect` 두 번). G7이 원본 `TopContainerLoadingBar::OnPaint`(`0x04c855a0`)를 읽어 고쳤다.

### 4-3. 그룹 사이 이관 — 6건, 전부 닫힘

| 무엇 | 어디서 → 어디로 | 결과 |
|---|---|---|
| `AsideAiTabsViewport` | G1 → G2 | 재현 완료 (동작은 L2) |
| `FloatingTabDragView` | G1 → G2 | 재현 완료 (동작은 L3) |
| 두 번째 `searchbox.mojom.Page` 연결 `0x0474d900` | G2 → (G4 경유) → G6 | 재현 완료 — Page 메서드 16개를 이벤트로 연결 |
| 확장 권한 문구 IDS 10347~10351 | G4 → G6 | 재현 완료 — 한 식구 6개를 같이 넣음(10346 포함). `en-US.pak`에서 문자열 확인 |
| 미니팝업의 프로필 전환 | G3 → G4 | `chrome::SwitchProfileInPlaceForAside()`를 부르면 됨. 창 토글이 L6에 걸려 있어 같이 확인 |
| 프로필 발치 바닥 고정 · 북마크 구역 프로필 재결속 | G3 → G1/G6 → G7 | 재현 완료 (G7 §4, §6-1) |

### 4-4. 코드로 확인된 결함 a~g (G4) — 7건 전부 처리

이전 담당이 코드는 이미 넣어 두었고, 이번에 **정말 도는가**를 증명했다.

| 결함 | 무엇이 문제였나 | 증거 |
|---|---|---|
| (a) | `$csp`가 URL 패턴만 키라 도메인 다른 규칙이 덮어씀 | 엔진 테스트 + 종단 `$csp: inline script blocked` |
| (b) | 렌더러가 빈 응답 20회 뒤 폴링 중단 | 단위 검사 5/5 + 종단 `late-added .ADBAR hidden` |
| (c) | 절차형 숨김 예외 `#@?#`가 무시됨 | 엔진 테스트 2건 + 종단 `procedural :has-text` 2건 |
| (d) | 모르는 스크립트 리소스가 빈 JS | 엔진 테스트 2건. 이제 널을 돌려준다 |
| (e) | `CancelImport`가 취소를 안 보냄 / `ImportEnded`가 실패도 성공 처리 | `aside_native_contracts_tests` 5/5 |
| (f) | 계정 API 프로필 번호 ≠ 미니팝업 해석 | `aside_account/aside_profile_index.h` 한 곳으로 통일 |
| (g) | 옴니박스 `AddFileContext`가 이미지를 버림 / `OpenLensSearch`가 확인만 함 | `aside_omnibox_context_unittests` 7/7, `aside_omnibox_action_unittests` 7/7 |

**증명하다 진짜 버그를 하나 찾았다.** 광고차단 코스메틱 주입이 실사용에서 통째로 죽어 있었다 —
`DidCreateDocumentElement()` 안에서 부른 `ExecuteScriptInIsolatedWorld`는 아무 것도 실행하지 않는다.
엔진이 데워진 평상시에는 페이로드가 거의 항상 먼저 도착하므로 **요소 숨김이 대부분의 문서에서
안 돌고 있었다.** 크로미움 확장이 쓰는 자리(`RunScriptsAtDocumentStart`)로 옮겨 고쳤다.
고치기 전 26/29 → 고친 뒤 **29/29**. (G4-RESULT §2)

### 4-5. HEAL — 재부팅 뒤 빌드 복구

기계 메모리 부족 재부팅으로 여섯 담당이 한꺼번에 죽었고, 트리에 **반쯤 저장된 편집**이 남았다.
HEAL 담당이 기능 추가 없이 컴파일·링크 오류만 고쳤다.

| 항목 | 값 |
|---|---|
| 고친 컴파일 오류 | **17개** (뿌리 파일 9개) |
| 고친 실행 중 죽던 문제 | **1개** — `PinnedTabNavigationThrottle`이 탭이 아닌 WebContents에서 널 참조로 SEGV |
| 주석 처리한 미완성 편집 | **0개** (전부 의도를 살려 고침) |
| 파급이 가장 컸던 것 | `aside_toolbar_collapse_hover_monitor.h`의 `raw_ptr` 하나 — 이 헤더 때문에 **16개 타깃**이 같이 깨졌다 |
| 남긴 확인 항목 | 2건 (색 하나, `deviceName` 하나) — 둘 다 G6·G5가 나중에 답을 냈다 |

HEAL이 G5 파일에 한 수정 4건은 G5가 대조해 **전부 의도와 맞다**고 확인했다(G5-RESULT §3).

---

## 5. 남은 일 (다음 회차 일감)

### 5-1. 코드로 못 끝내는 것 — 9건

| # | 무엇 | 왜 |
|---|---|---|
| X1 | pq_v1 서명 알맹이 | 맥OS 26 ML-DSA + 키체인 접근 그룹 `8CPD4K4TBB.at.studio.AsideBrowser.cryptography`. 리눅스에 만들 수 있는 물건이 아니다. 스킴 판정·거절 경로·문구는 이식 완료 |
| X2 | 맥 업데이터 `0x081cc570` / `0x081c06c0` | `enable_updater`를 켜면 buildflags가 바뀌어 트리 전체가 재빌드된다. 켜도 리눅스 구현은 껍데기 |
| X3 | AsideWebsiteStorage 타입을 **어디서 켜는지** | 원본도 어느 묶음에도 안 넣는다(바이너리로 확인). 우리도 같다 — **결함이 아니라 원본 그대로** |
| X4 | `AsideWebsiteStorageSyncService` 브리지·컨트롤러 | G5 범위 밖. 만들면 `aside_website_storage_kinds`가 링커에 안 버려진다 |
| X5 | 데몬 access token이 sync 엔진에 안 물림 | 원본이 어디서 갈아끼우는지 아직 못 밝힘 |
| X6 | `AsideCookieSyncService` | G5 범위 밖. 상위 ChromeOS `floating_sso`를 데스크톱으로 옮긴 것으로 보인다 |
| X7~X9 | 정체 미상 4군데 (`0x045dd200`, `0x049ca150`, `0x047c51f0`, `0x0d145880`) | 위 §2. 맥 빌드 비교만이 남은 길 |

### 5-2. 그 밖에 남은 차이

- **픽셀 동일성 (HANDOFF A8)**: 원본이 맥 앱이라 스크린샷을 못 얻는다. 바이트로 잴 수 없어 위 표에
  없다. **다만 오늘 66색 팔레트가 들어와 색은 근사가 아니라 원본 값이 되었다** — 남은 것은 여백·모서리다.
- **주소창 진행 표시 보간**: 원본은 `gfx::Animation` + `Tween::EASE_OUT`, 우리는 500 ms 눈금마다
  남은 거리의 25%. 색·투명도는 추정 (G2-RESULT §3-5).
- **고정 칩 끌어 옮기기**: `PinnedEntryDragGhost`·`FinishEntryDrag` 이름만 남아 있고 동작 근거가 없다.
- **고정 탭 스로틀의 판정 규칙**: 원본의 정확한 조건식이 문자열로 복원되지 않는다 (G1-RESULT §2-8).
- **`AsideAiTabsViewport`의 대상 고르기**: 원본이 세 곳을 검사하는데 둘째·셋째가 무엇인지 미확정.
- **컴포넌트 publisher 키 요구 방식**: 우리는 `crx_verifier.cc` 한 줄, 원본은 컴포넌트별 요구였을
  가능성 (G5-RESULT §1-가).

### 5-3. 라이브 확인 10건

위 §3. 호스트를 올릴 때 그대로 실행하면 된다.

---

## 6. 오늘의 작업 구조 (뒤에 오는 사람을 위해)

| 회차 | 담당 | 범위 | 결과 문서 | 패치 |
|---|---|---|---|---|
| HEAL | 빌드 복구 | 컴파일·링크 오류만 | `campaign/HEAL-RESULT.md` | (패치 없음, 다른 그룹 패치에 포함) |
| G1 | 세로탭·탭 | 14군데 788,800 B | `campaign/G1-RESULT.md` | `patches/041-G1.patch` (26파일) |
| G2 | 프레임·툴바·주소창·분할 보기 | 10군데 490,672 B | `campaign/G2-RESULT.md` | `patches/041-G2.patch` (24파일) |
| G3 | 프로필·알림·확장 UI | 10군데 385,712 B | `campaign/G3-RESULT.md` | `patches/041-G3.patch` (31파일) |
| G4 | 광고차단·가져오기·보안 CDP | 5군데 281,520 B + 결함 a~g | `campaign/G4-RESULT.md` | `patches/041-G4.patch` (170파일) |
| G5 | sync·업데이터 | 14군데 406,400 B | `campaign/G5-RESULT.md` | `patches/041-G5.patch` (37파일) |
| G6 | 정체 미상·겉만 + 추가 후보 6 + 이관 | 7군데 202,912 B + 8건 | `campaign/G6-RESULT.md` | `patches/041-G6.patch` (55파일) |
| G7 | 색 66개 + 그룹 밖 잔여 | 구간 배정 없음 | `campaign/G7-RESULT.md` | `patches/041-G7.patch` (19파일) |

**최종 전체 패치**: `patches/042-full-fork-2026-09-06.patch`
— 548파일, +44,126 −112, 바탕 `cc5584af0d` 대비, sha256 앞 16자리 `53cc80c4a5f40099`.
경고 하나가 §7에 있다.

캡처는 `ui-shots/G1-*` ~ `ui-shots/G7-*` **93장**.
차등 기록은 `DIFFERENTIAL.md` (12)~(18)절.

---

## 7. 문서끼리 어긋난 곳 — 전부 적는다

이 지도는 아래 판정을 따랐다. 어긋난 채로 남겨 두지 않고, 어느 쪽을 왜 골랐는지 적는다.

1. **`0x0474d900` (주소창 제안 UI)** — G2 §5는 **불가**로 닫았다. 그 뒤 G6 §5(14)가 속을 채워
   **재현 완료**로 바꿨다. G2의 이유("두 번째 연결의 주인이 내 담당이 아니다")가 G6에서 해소된 것이라
   충돌이 아니라 **인계**다. → 지도는 재현 완료.

2. **`0x04c6a630` (가로 고정 탭 칩)** — G1 §9는 **미해결**로 남기며 "레이어·그리기 순서를 파고들어야
   한다"고 적었다. G7 §5가 원인을 밝혔다 — 배치가 아니라 `GetChildrenInZOrder()` 목록이었다.
   **G1이 의심한 방향은 틀렸고 되돌린 것이 옳았다.** → 지도는 재현 완료.

3. **`0x047c9720` (Geist·headless·AppleScript)** — G3 머리글은 "재현 완료 7 / 상위에 이미 있음 2 /
   불가 1"이라 적었지만, **G3 자기 표를 세면 재현 8 / 상위 1 / 불가 1이다.** 머리글은 이 구간의 세
   갈래 중 둘이 상위 코드라는 이유로 "상위"에 넣은 듯하다. 지도는 **표를 따랐다** — 이 구간에서
   Aside 고유인 것은 Geist뿐이고 그것은 재현됐다.

4. **`0x081c9f90` (업데이터 mojo 바인딩)** — G5 §0 표는 "완료 10"에 넣었으나 내용은 "정체 밝힘 +
   이미 있음"이다. 지도는 **"상위에 이미 있음"으로 따로 센다**(새로 만든 것이 없으므로).
   합계에는 영향이 없다.

5. **`0x0d186f70` (세로탭 hover 마우스 속도)** — 아침 지도는 "안 만듦", VERSION-STEPS는 "재현 완료"로
   서로 달랐다. G1 §3이 실측으로 **VERSION-STEPS가 맞다**고 판정했다. → 지도는 "원본과 차이 없음 확인".
   다만 원본의 기능 매개변수 **기본값 자체는 바이너리에서 못 읽었다** — 미확인으로 남는다.

6. **`0x04bbe2f0` (맥 확장 팝오버)** — 아침 지도는 "확인 후 필요 없음으로 옮길 것"이라 적었고,
   G3 §5가 확인해 "**불가 — 맥 전용**(리눅스 대응 동작은 상위에 이미 있음)"으로 닫았다.
   지도는 **G3의 표현대로 불가에 둔다.** 실질은 "필요 없음"과 같다 — 만들 것이 없다는 뜻이므로,
   비율을 볼 때는 이 35,424 B(1.08%)를 "필요 없음"으로 읽어도 된다.

7. **북마크 줄 아이콘 색이 세 번 바뀌었다** — HEAL이 없어진 `kColorIconActive`를 `kColorSysPrimary`로
   고쳤고(HEAL §4), 다음 담당이 `kColorPrimaryForeground`로 다시 고쳤고(G6이 검증해 맞다고 판정),
   G7이 원본 이름표 `kColorAsideSurfacePrimaryForeground`로 마지막에 바꿨다. **최종은 G7의 것**이고,
   G6의 판단 근거(강조색을 주면 다른 줄과 차이가 안 난다)는 여전히 유효하다.

8. **최종 패치 042에 파일 하나가 빠졌다.** 작업 트리는 바탕 대비 **548파일**인데 패치는 **548파일**이다.
   빠진 것은 `third_party/blink/public/devtools_protocol/domains/Browser.pdl` (20줄) —
   Aside의 CDP 명령 `Browser.ensureProfile`·`Browser.setAiTabsMetadata` **선언**이다.
   패치 042에는 `third_party/` 경로가 하나도 없다. 이 hunk는 `patches/041-G4.patch`에만 있다.
   → **042만 적용하면 보안 원격 디버깅의 두 명령이 선언되지 않는다.** 042 + 041-G4의 Browser.pdl
   → **해결됨(17:45, 오케스트레이터)**: 042를 `git diff cc5584af0d -- . ':(exclude)out'`로 다시 만들어 `Browser.pdl`을 포함시켰다. 548파일, sha256 앞 16자리 `53cc80c4a5f40099`.
   조각을 같이 적용하거나, 042를 다시 뜰 것.

9. **미커밋 파일 수** — 오케스트레이터 지시문은 "≈309개"라 했으나, 17:22 실측은 **HEAD 대비 448개**다
   (`git diff HEAD --name-only`). 309는 낮 시간대 값으로 보인다. 바탕 대비 총 548개
   (= HEAD 커밋 `1d00f53b` 117개 + 미커밋 448개, 겹치는 파일 있음).

10. **G6 자기 집계가 자기 표와 어긋난다.** G6 머리글은 "15개 중 **12개 재현 완료, 3개 불가**"라 적었지만,
    G6 자기 표를 세면 **재현 10 · 정체 규명 1 · 불가 4**다. 머리글은 "반쯤 좁힌" `0x0d145880`을
    불가에서 뺀 듯하다(같은 문서 §3이 "1군데 성공, 1군데 반쯤, 3군데 실패"라 적는다).
    지도는 **표를 따랐다** — `0x04c65910`은 새로 만들 것이 없으므로 재현 완료,
    `0x0d145880`은 계통만 좁혔고 이름을 못 얻었으므로 불가. → 재현 11 · 불가 4.

---

## 8. 이 표를 믿을 때 붙는 단서

아침 판의 단서 중 아직 살아 있는 것과, 오늘 해소된 것을 갈라 적는다.

**아직 살아 있다.**

1. **독립 재현을 안 했다.** 구간 경계는 compact-unwind 하나에만 기댄다. 두 번째 디스어셈블러로
   다시 보기 전에는 이 구간 나눔을 `VERIFIED`라 부르지 말 것.
2. **매처가 헛짚는다는 것이 오늘 증명됐다** — G2 §4-10이 `ToggleButton` 구간에서 원본·기준선·우리
   트리가 완전히 같은데도 블록 분류가 `unmatched 16 / matched 6`인 것을 보였다. 그래서 "차이 없음
   확인" 9군데(4.64%)는 **원래부터 차이가 없던 자리**다. 바이트를 성적표로 읽으면 안 된다.
3. **바이트는 "안 맞는 블록"의 크기다.** 우리가 쓴 코드의 양이 아니다.
4. **조밀 구간 밖의 불일치 10.7 MB는 분해하지 못했다.** 그중 17.3%는 dawn·skia·perfetto 같은
   Aside가 손댈 이유 없는 코드 — 잡음 하한이다.
5. **주소 좌표계가 다르다.** 이 지도는 x86_64, 우리 RE 문서는 arm64다.
6. **"재현 완료"는 "바이트가 같다"가 아니다.** 대부분은 원본 문자열·명령 번호·구조를 맞춘 것이고,
   안쪽은 상위 크로미움 코드를 쓴다. 광고 차단 엔진이 대표적이다(차단 결과 29/29 통과, 엔진 속은 다름).

**오늘 해소됐다.**

7. 아침의 문서 충돌 1건(`0x0d186f70`)은 G1이 실측으로 닫았다 (§7-5).
8. 아침의 "못 알아냄 7군데" 중 **3군데는 정체가 밝혀졌다** — `0x0afc71e0`(ToggleButton, G2),
   `0x04c65910`(고정 탭 칩 계통, G6), `0x081c9f90`(업데이터 mojo 바인딩, G5). 4군데가 남았다.
9. 아침의 "겉만 1군데"(`0x048f2710`)는 G6이 함수 63개를 문자열 대조해 속을 채웠다.

---

## 9. 이 문서가 기대는 근거 파일

**오늘의 결과 문서 (가장 정확).**

- `belmont-browse/aside-fork/campaign/COMMON.md` — 공통 규칙
- `belmont-browse/aside-fork/campaign/HEAL-RESULT.md` — 빌드 복구
- `belmont-browse/aside-fork/campaign/G1-RESULT.md` ~ `G7-RESULT.md` — 구간별 판정·증거·남은 차이
- `belmont-browse/aside-fork/DIFFERENTIAL.md` (12)~(18)절 — 증분별 근거
- `belmont-browse/aside-fork/HANDOFF-2026-09-05.md` §7 — 7차 세션 인수인계

**패치.**

- `belmont-browse/aside-fork/patches/041-G1.patch` ~ `041-G7.patch` — 그룹별 스냅샷
- `belmont-browse/aside-fork/patches/042-full-fork-2026-09-06.patch` — 최종 전체 (§7-8 경고)

**원자료 (본 표의 모든 숫자).**

- `data/artifacts/aside_fork_map_825_20260906/analysis/patch-map.json` — 구간 76개
- `data/artifacts/aside_version_steps_20260906/analysis/step-labels.json` — 버전 짝 이름표
- `data/artifacts/aside_component_history_20260906/` — 확장·데몬 24개 판
- `data/artifacts/aside_latest_engine_20260906/` — 905 후보

**아침의 분석 보고서 4개 (이 지도의 바탕).**

- `usecases/FORK-MAP-825-2026-09-06.md` — 본 표의 출처
- `usecases/VERSION-STEPS-2026-09-06.md` — 추가 후보 6개
- `usecases/API-HISTORY-2026-09-06.md` — 확장 API 시간표 (남은 일 0)
- `usecases/ENGINE-905-CANDIDATE-2026-09-06.md` — 905 후보 판정 (앵커 8/8)

**우리 포크 상태 문서.**

- `belmont-browse/aside-fork/UI-PLAN.md` — UI 상태표, 원본 소스 파일 지도
- `usecases/PARITY-RESULTS-2026-09-05.md` — 실사용 검사 15/15

---

## 부록 — 아침 판(09:30)의 본 표

아침에는 상태가 다섯이었고 집계가 이랬다. 대조용으로만 남긴다.

| 아침 상태 | 구간 수 | 바이트 | 비율 | → 오늘 어떻게 됐나 |
|---|---|---|---|---|
| 안 만듦 (미재현) | 31 | 1,347,760 | 41.23% | 재현 완료 23 · 불가(맥·윈) 5 · 차이 없음 3 |
| 덜 만듦 (부분) | 21 | 1,008,608 | 30.86% | 재현 완료 15 · 차이 없음 5 · 상위에 있음 1 |
| 맞게 만듦 (재현) | 13 | 627,712 | 19.20% | 그대로 (4군데는 G7이 색을 원본 값으로 교체) |
| 맞게 만듦, 표면만 | 1 | 147,040 | 4.50% | **재현 완료** (G6이 속을 채움) |
| 못 알아냄 (미확인) | 7 | 52,608 | 1.61% | 정체 밝힘 3 (재현 1 · 차이 없음 1 · 상위에 있음 1) · 불가 4 |
| 필요 없음 (해당없음) | 3 | 84,976 | 2.60% | 그대로 |
